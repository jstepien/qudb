module Database.QUDB.Query (
    -- query
	) where

import Database.QUDB.EntityTypes
import Database.QUDB.Structure

-- query :: (ExecutableQuery eq) => DB -> [eq] -> IO [[Value]]
-- query _ _ = retrun [[Value]] 
